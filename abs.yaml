# TODO: Windows code signing is currently disabled as it breaks the build.
# Similar to https://github.com/bazelbuild/bazel/issues/600 when the binary is stripped.
# This should be removed once https://anaconda.atlassian.net/browse/PKG-5977 is unblocked.
content_signing:
    win-64: False

aggregate_check: false

build_parameters:
  - "--suppress-variables"
  - "--skip-existing"

channels:
  - https://staging.continuum.io/pbp/fs/bazel-feedstock/pr0/aa2b3af
  - https://staging.continuum.io/pbp/fs/ijar-feedstock/pr2/afd8d72
  - https://staging.continuum.io/pbp/fs/singlejar-feedstock/pr2/b510032
  - https://staging.continuum.io/pbp/fs/grpc_java_plugin-feedstock/pr3/ff2af0d